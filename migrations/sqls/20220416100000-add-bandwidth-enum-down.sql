-- Revert add Bandwidth enum value
-- --------------------------------------------

-- Dropping values is not possible and the workaround is very very difficult. So we do not :'(
-- alter type sms.profile_service_option drop value 'bandwidth';
